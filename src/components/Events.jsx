import React from 'react';
import "./Events.css"

const Events = () => {
    return (
        <events>
            <h1>Upcoming Events</h1>
            {/* Add events content here */}
        </events>
    );
};

export default Events;